import { FC } from "react";

type TCalenderProps = object;

const Calender: FC<TCalenderProps> = () => {
  return (
    <div>
      <h2>This is a Calender component</h2>
    </div>
  );
};

export default Calender;
